{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "go",
      "request": "launch",
      "name": "Debug MRSequential",
      "program": "${workspaceFolder}/src/main/mrsequential.go",
      "cwd": "${workspaceFolder}/src/main",
      "args": ["../mrapps/wc.so", "pg-being_ernest.txt", "pg-dorian_gray.txt", "pg-sherlock_holmes.txt"],
      // "debugAdapter": "dlv-dap",
      "preLaunchTask": "build-plugin"
    }
  ]
}